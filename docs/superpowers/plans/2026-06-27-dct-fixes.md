# dct 패키지 보강 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `dct` 패키지를 정규직교 기본 normalization, fp16/dtype 지원, 구조 정리, IDCT 입력 검증, pytest 스위트로 보강한다(v2.0.0).

**Architecture:** `DCT2d`/`IDCT2d`를 하나의 `_BlockTransform` 베이스로 통합해 2D zigzag 커널을 단 한 번 등록한다. `norm` 인자를 scipy로 전달하고(기본 ortho), forward에서 커널을 입력 dtype로 캐스팅한다. 정/역 라운드트립을 만드는 수학적 구성(전치 비대칭, kron, zigzag)은 보존하고 회귀 테스트로 보장한다.

**Tech Stack:** Python 3.10+, PyTorch ≥2.0, numpy, scipy, pytest.

## Global Constraints

- `requires-python = ">=3.10"`; `torch>=2.0`, `numpy`, `scipy` 런타임 의존성 유지.
- 공개 API: `__all__ = ["DCT2d", "IDCT2d"]` 만 노출.
- 버전은 `pyproject.toml`과 `dct/__init__.py` 양쪽 모두 `2.0.0`로 일치.
- `norm` 허용값은 정확히 `("ortho", "backward", "forward")`; 그 외엔 `ValueError`.
- 동일 `norm`에서 `IDCT2d(DCT2d(x)) == x` 라운드트립 정확성 유지(ortho 오차 < 1e-4).
- 커널 buffer 기본 dtype은 `float32`; forward에서 입력 dtype에 맞춰 캐스팅.
- 테스트는 `cpu`에서 항상 통과; `mps`/`cuda`는 가용 시 추가 검증(없으면 skip).

---

### Task 1: pytest 스캐폴드 + `norm` 인자 (기본 ortho)

**Files:**
- Create: `tests/test_dct.py`
- Modify: `dct/dct.py` (helper 시그니처에 `norm` 추가, `DCT2d`/`IDCT2d`/베이스에 `norm` 인자)

**Interfaces:**
- Produces:
  - `DCT2d(dct=2, kernel_size=8, selections=None, norm="ortho", device=None)`
  - `IDCT2d(dct=2, kernel_size=8, selections=None, norm="ortho", device=None)`
  - 모듈 상수 `_VALID_NORMS = ("ortho", "backward", "forward")`
  - 헬퍼 `_zigzag_permutation(k, device=None)` (테스트에서 zigzag 역정렬에 사용)

- [ ] **Step 1: 실패 테스트 작성** — `tests/test_dct.py` 생성

```python
import numpy as np
import pytest
import scipy.fft
import torch

from dct import DCT2d, IDCT2d
from dct.dct import _zigzag_permutation

DEVICES = ["cpu"] + (["mps"] if torch.backends.mps.is_available() else []) \
                  + (["cuda"] if torch.cuda.is_available() else [])


@pytest.mark.parametrize("device", DEVICES)
@pytest.mark.parametrize("norm", ["ortho", "backward"])
@pytest.mark.parametrize("k", [4, 8, 16])
def test_roundtrip_is_exact(device, norm, k):
    fwd = DCT2d(kernel_size=k, norm=norm).to(device)
    inv = IDCT2d(kernel_size=k, norm=norm).to(device)
    x = torch.randn(2, 3, 2 * k, 2 * k, device=device)
    recon = inv(fwd(x))
    assert recon.shape == x.shape
    assert (recon - x).abs().max().item() < 1e-4


@pytest.mark.parametrize("device", DEVICES)
@pytest.mark.parametrize("k", [4, 8])
def test_basis_is_orthonormal_for_ortho(device, k):
    fwd = DCT2d(kernel_size=k, norm="ortho").to(device)
    ker = fwd.kernel.reshape(k * k, k * k).cpu()
    gram = ker @ ker.T
    assert (gram - torch.eye(k * k)).abs().max().item() < 1e-4


def test_matches_scipy_dctn_for_ortho():
    k = 8
    fwd = DCT2d(kernel_size=k, norm="ortho")
    block = torch.randn(1, 1, k, k)
    mine = fwd(block).reshape(-1).numpy()           # zigzag 순서
    perm = _zigzag_permutation(k).numpy()
    grid = np.empty(k * k, np.float32)
    grid[perm] = mine
    grid = grid.reshape(k, k)
    ref = scipy.fft.dctn(block.numpy().reshape(k, k), type=2, norm="ortho",
                         orthogonalize=True)
    assert np.abs(grid - ref).max() < 1e-4


def test_invalid_norm_raises():
    with pytest.raises(ValueError):
        DCT2d(norm="nope")
    with pytest.raises(ValueError):
        IDCT2d(norm="nope")
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `.venv/bin/python -m pytest tests/test_dct.py -q`
Expected: FAIL — `DCT2d(norm=...)`가 `TypeError: unexpected keyword argument 'norm'`, orthonormal/scipy 테스트도 실패(현재 backward 스케일).

- [ ] **Step 3: `dct/dct.py`에 norm 스레딩 구현** — helper와 클래스 시그니처 수정

`_VALID_NORMS` 상수를 파일 상단(`Tensor = torch.Tensor` 아래)에 추가:

```python
_VALID_NORMS = ("ortho", "backward", "forward")
```

두 헬퍼에 `norm` 인자 추가:

```python
def _initialize_dct_kernel(d: int, k: int, norm: str,
                           device: str | torch.device = None) -> Tensor:
    return torch.as_tensor(scipy.fft.dct(np.eye(k), d, norm=norm, orthogonalize=True),
                           dtype=torch.float32, device=device)


def _initialize_inverse_dct_kernel(d: int, k: int, norm: str,
                                   device: str | torch.device = None) -> Tensor:
    return torch.as_tensor(scipy.fft.idct(np.eye(k), d, norm=norm, orthogonalize=True),
                           dtype=torch.float32, device=device)
```

`_DCT.__init__`/`_IDCT.__init__`에 `norm` 인자를 추가하고 검증 후 전달:

```python
class _DCT(nn.Module):
    def __init__(self, dct: int, kernel_size: int, norm: str,
                 device: str | torch.device = None) -> None:
        super().__init__()
        if norm not in _VALID_NORMS:
            raise ValueError(f"norm must be one of {_VALID_NORMS}, got {norm!r}")
        self.dct = dct
        self.kernel_size = kernel_size
        self.norm = norm
        self.register_buffer("kernel", _initialize_dct_kernel(dct, kernel_size, norm, device).T)
```

`_IDCT.__init__`도 동일하게(검증 + `self.norm = norm`), 단 `.T` 없이
`_initialize_inverse_dct_kernel(dct, kernel_size, norm, device)` 등록.

`DCT2d.__init__`/`IDCT2d.__init__`에 `norm="ortho"` 인자를 추가하고 super로 전달:

```python
class DCT2d(_DCT):
    def __init__(self, dct: int = 2, kernel_size: int = 8, selections: int | None = None,
                 norm: str = "ortho", device: str | torch.device = None) -> None:
        super().__init__(dct, kernel_size, norm, device)
        # ... 기존 본문 동일 ...
```

```python
class IDCT2d(_IDCT):
    def __init__(self, dct: int = 2, kernel_size: int = 8, selections: int | None = None,
                 norm: str = "ortho", device: str | torch.device = None) -> None:
        super().__init__(dct, kernel_size, norm, device)
        # ... 기존 본문 동일 ...
```

`extra_repr`에 norm 포함(두 베이스 모두):

```python
    def extra_repr(self) -> str:
        return f"dct={self.dct}, kernel_size={self.kernel_size}, norm={self.norm!r}"
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `.venv/bin/python -m pytest tests/test_dct.py -q`
Expected: PASS (전 항목).

- [ ] **Step 5: 커밋**

```bash
git add tests/test_dct.py dct/dct.py
git commit -m "feat: add norm arg (orthonormal by default) with tests"
```

---

### Task 2: 구조 정리 — 단일 버퍼 베이스로 통합, 죽은 코드 제거

**Files:**
- Modify: `dct/dct.py` (베이스 클래스 통합, helper 단순화, `reparameterize` 제거)
- Modify: `tests/test_dct.py` (단일 kernel 버퍼 + repr 회귀 테스트 추가)

**Interfaces:**
- Consumes: Task 1의 `DCT2d`/`IDCT2d` 시그니처와 `_VALID_NORMS`.
- Produces:
  - 베이스 클래스 `_BlockTransform(nn.Module)` (내부용)
  - 헬퍼 `_build_block_kernel(matrix_1d, k)` (내부용)
  - `module._weight(x)` (forward에서 dtype 정합 커널 반환; Task 3에서 사용)
  - `module.selections` 정수 속성(DCT2d·IDCT2d 공통)

- [ ] **Step 1: 회귀 테스트 추가** — `tests/test_dct.py` 끝에 append

```python
def test_single_kernel_buffer_and_repr():
    fwd = DCT2d(kernel_size=8, norm="ortho")
    # 'kernel' 버퍼가 정확히 하나, 2D 형태(k², 1, k, k)
    buffers = dict(fwd.named_buffers())
    assert list(buffers.keys()) == ["kernel"]
    assert tuple(buffers["kernel"].shape) == (64, 1, 8, 8)
    assert "norm='ortho'" in repr(fwd)
    # selections 속성 노출
    assert DCT2d(kernel_size=8).selections == 64
    assert DCT2d(kernel_size=8, selections=10).selections == 10
```

- [ ] **Step 2: 회귀 테스트가 (대부분) 실패함을 확인**

Run: `.venv/bin/python -m pytest tests/test_dct.py::test_single_kernel_buffer_and_repr -q`
Expected: FAIL — 현재 `DCT2d`에 `selections` 속성이 없어 `AttributeError`.

- [ ] **Step 3: `dct/dct.py`를 통합 베이스로 리팩터**

`# Checking` 섹션의 헬퍼를 단순화:

```python
def _is_a_batched_tensor(x: Tensor) -> bool:
    return x.ndim == 4


def _is_resolution_divisible(x: Tensor, d: int) -> bool:
    return x.size(2) % d == 0 and x.size(3) % d == 0
```

`# Helper Functions` 섹션에 블록 커널 빌더 추가(zigzag/device를 일원화):

```python
def _build_block_kernel(matrix_1d: Tensor, k: int) -> Tensor:
    # Kronecker product로 분리형 2D 기저를 만들고 (k², 1, k, k)로 펼친 뒤
    # zigzag(저→고주파) 순서로 재배열한다. zigzag 인덱스는 커널과 동일 디바이스.
    kernel = _expand_kernel(matrix_1d, 2).reshape(k ** 2, 1, k, k)
    return kernel[_zigzag_permutation(k, kernel.device)]
```

`# Abstract Classes` 섹션의 `_DCT`/`_IDCT`를 **삭제**하고 단일 베이스로 교체:

```python
class _BlockTransform(nn.Module):

    def __init__(self, dct: int, kernel_size: int, selections: int | None, norm: str,
                 kernel_fn, transpose: bool, device: str | torch.device = None) -> None:
        super().__init__()
        if norm not in _VALID_NORMS:
            raise ValueError(f"norm must be one of {_VALID_NORMS}, got {norm!r}")
        self.dct = dct
        self.kernel_size = kernel_size
        self.norm = norm

        # 1D (역)DCT 행렬을 만든다. 분석(DCT)은 전치한 행렬을 상관(conv2d) 필터로,
        # 합성(IDCT)은 비전치 행렬을 전치합성곱 필터로 사용한다 — 이 비대칭이
        # 정/역 라운드트립을 성립시킨다.
        matrix_1d = kernel_fn(dct, kernel_size, norm, device)
        if transpose:
            matrix_1d = matrix_1d.T

        kernel = _build_block_kernel(matrix_1d, kernel_size)
        self.selections = kernel_size ** 2 if selections is None else selections
        if selections is not None:
            kernel = kernel[:selections]
        self.register_buffer("kernel", kernel)

    def _weight(self, x: Tensor) -> Tensor:
        return self.kernel if self.kernel.dtype == x.dtype else self.kernel.to(x.dtype)

    def forward(self, x: Tensor) -> Tensor:
        raise NotImplementedError

    def extra_repr(self) -> str:
        return (f"dct={self.dct}, kernel_size={self.kernel_size}, "
                f"selections={self.selections}, norm={self.norm!r}")
```

`# Modules` 섹션의 `DCT2d`를 베이스 사용으로 교체(이중 등록 제거, `reparameterize` 삭제):

```python
class DCT2d(_BlockTransform):

    def __init__(self, dct: int = 2, kernel_size: int = 8, selections: int | None = None,
                 norm: str = "ortho", device: str | torch.device = None) -> None:
        super().__init__(dct, kernel_size, selections, norm,
                         _initialize_dct_kernel, transpose=True, device=device)

    def forward(self, x: Tensor) -> Tensor:
        if not _is_a_batched_tensor(x):
            raise RuntimeError(f"{self.__class__.__name__} only supports batched image tensors; "
                               f"expected a 4D tensor but received a {x.ndim}D tensor")
        if not _is_resolution_divisible(x, self.kernel_size):
            raise RuntimeError("the resolution must be divisible by the kernel size")

        b, c, h, w = x.shape
        x = x.reshape(b * c, 1, h, w)
        x = F.conv2d(x, self._weight(x), None, self.kernel_size)
        x = x.reshape(b, -1, h // self.kernel_size, w // self.kernel_size)
        return x
```

`IDCT2d`도 교체:

```python
class IDCT2d(_BlockTransform):

    def __init__(self, dct: int = 2, kernel_size: int = 8, selections: int | None = None,
                 norm: str = "ortho", device: str | torch.device = None) -> None:
        super().__init__(dct, kernel_size, selections, norm,
                         _initialize_inverse_dct_kernel, transpose=False, device=device)

    def forward(self, x: Tensor) -> Tensor:
        if not _is_a_batched_tensor(x):
            raise RuntimeError(f"{self.__class__.__name__} only supports batched coefficient "
                               f"tensors; expected a 4D tensor but received a {x.ndim}D tensor")

        b, n, h, w = x.shape
        c = n // self.selections
        x = x.reshape(b * c, -1, h, w)
        x = F.conv_transpose2d(x, self._weight(x), None, self.kernel_size)
        x = x.reshape(b, c, h * self.kernel_size, w * self.kernel_size)
        return x
```

- [ ] **Step 4: 전체 테스트 통과 확인(회귀 없음)**

Run: `.venv/bin/python -m pytest tests/test_dct.py -q`
Expected: PASS (Task 1 테스트 전부 + 신규 단일버퍼 테스트).

- [ ] **Step 5: 커밋**

```bash
git add dct/dct.py tests/test_dct.py
git commit -m "refactor: unify DCT/IDCT into single-buffer base, drop dead code"
```

---

### Task 3: fp16/dtype — forward 커널 캐스팅

**Files:**
- Modify: `tests/test_dct.py` (dtype 테스트 추가)
- Modify: `dct/dct.py` (이미 Task 2에서 `_weight` 추가됨 — forward가 이를 사용)

**Interfaces:**
- Consumes: Task 2의 `module._weight(x)`.

- [ ] **Step 1: 실패 테스트 작성** — `tests/test_dct.py`에 append

```python
def test_double_precision_roundtrip():
    fwd = DCT2d(kernel_size=8, norm="ortho").double()
    inv = IDCT2d(kernel_size=8, norm="ortho").double()
    x = torch.randn(1, 3, 16, 16, dtype=torch.float64)
    recon = inv(fwd(x))
    assert recon.dtype == torch.float64
    assert (recon - x).abs().max().item() < 1e-10


def test_half_precision_input():
    # 커널 buffer는 float32, 입력은 float16 — forward 캐스팅으로 동작해야 함.
    devs = [d for d in DEVICES if d != "cpu"]  # CPU는 half conv 미지원
    if not devs:
        pytest.skip("no half-capable device")
    dev = devs[0]
    fwd = DCT2d(kernel_size=8, norm="ortho").to(dev)
    out = fwd(torch.randn(1, 3, 16, 16, device=dev, dtype=torch.float16))
    assert out.dtype == torch.float16
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `.venv/bin/python -m pytest tests/test_dct.py::test_double_precision_roundtrip tests/test_dct.py::test_half_precision_input -q`
Expected: `test_double_precision_roundtrip` FAIL.

> 참고: Task 2에서 `_weight`를 forward에 이미 연결했다면 이 테스트가 이미 통과할 수 있다. 그 경우 `_weight` 메커니즘이 dtype 정합을 보장하는지 테스트가 명시적으로 검증하는 것이므로, 통과를 확인하고 Step 5로 진행한다(추가 구현 불필요).

- [ ] **Step 3: (필요 시) forward 캐스팅 확인/보정**

`DCT2d.forward`/`IDCT2d.forward`가 `F.conv2d(..., self._weight(x), ...)` /
`F.conv_transpose2d(..., self._weight(x), ...)`로 커널을 전달하는지 확인.
`self.kernel`을 직접 쓰고 있다면 `self._weight(x)`로 교체.

- [ ] **Step 4: 테스트 통과 확인**

Run: `.venv/bin/python -m pytest tests/test_dct.py -k "precision" -q`
Expected: PASS (half는 디바이스 없으면 skip).

- [ ] **Step 5: 커밋**

```bash
git add dct/dct.py tests/test_dct.py
git commit -m "feat: support fp16/fp64 via forward kernel casting"
```

---

### Task 4: IDCT2d 입력 검증

**Files:**
- Modify: `tests/test_dct.py` (검증 에러 테스트 추가)
- Modify: `dct/dct.py` (`IDCT2d.forward`에 채널 가분성 검증 추가)

**Interfaces:**
- Consumes: Task 2의 `IDCT2d.forward`, `module.selections`.

- [ ] **Step 1: 실패 테스트 작성** — `tests/test_dct.py`에 append

```python
def test_dct_rejects_bad_input():
    fwd = DCT2d(kernel_size=8)
    with pytest.raises(RuntimeError):
        fwd(torch.randn(3, 32, 32))          # 4D 아님
    with pytest.raises(RuntimeError):
        fwd(torch.randn(1, 3, 30, 30))       # 해상도 비가분


def test_idct_rejects_bad_input():
    inv = IDCT2d(kernel_size=8, selections=16)   # 채널은 16의 배수여야 함
    with pytest.raises(RuntimeError):
        inv(torch.randn(3, 16, 4, 4)[0])         # 3D → 4D 아님
    with pytest.raises(RuntimeError):
        inv(torch.randn(2, 24, 4, 4))            # 24 % 16 != 0 → 채널 비가분
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `.venv/bin/python -m pytest tests/test_dct.py::test_idct_rejects_bad_input -q`
Expected: FAIL — 채널 비가분(24 % 16) 케이스에서 현재 예외 없이 통과(조용히 floor).

- [ ] **Step 3: `IDCT2d.forward`에 검증 추가**

`b, n, h, w = x.shape` 다음에 가분성 검증 삽입:

```python
        b, n, h, w = x.shape
        if n % self.selections != 0:
            raise RuntimeError(f"the channel dimension ({n}) must be divisible by "
                               f"selections ({self.selections})")
        c = n // self.selections
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `.venv/bin/python -m pytest tests/test_dct.py -k "reject" -q`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add dct/dct.py tests/test_dct.py
git commit -m "feat: validate IDCT2d channel divisibility"
```

---

### Task 5: 커버리지 테스트 + docstring + 메타데이터(v2.0.0)

**Files:**
- Modify: `tests/test_dct.py` (selections·autograd 테스트 추가)
- Modify: `dct/dct.py` (`DCT2d`/`IDCT2d` docstring)
- Modify: `pyproject.toml` (version 2.0.0, dev 의존성 pytest)
- Modify: `dct/__init__.py` (`__version__ = "2.0.0"`)
- Modify: `README.md` (normalization 한 줄)

**Interfaces:**
- Consumes: Task 1~4의 공개 API.

- [ ] **Step 1: 커버리지 테스트 작성** — `tests/test_dct.py`에 append

```python
@pytest.mark.parametrize("sel", [64, 32, 16, 8])
def test_selections_channel_count(sel):
    fwd = DCT2d(kernel_size=8, selections=sel)
    out = fwd(torch.randn(2, 3, 32, 32))
    assert out.shape[1] == 3 * sel


def test_selections_monotonic_rmse():
    x = torch.randn(2, 3, 32, 32)
    rmses = []
    for sel in (64, 32, 16, 8):
        f = DCT2d(kernel_size=8, selections=sel, norm="ortho")
        i = IDCT2d(kernel_size=8, selections=sel, norm="ortho")
        rmses.append((i(f(x)) - x).pow(2).mean().sqrt().item())
    assert rmses[0] < 1e-4                       # 전 계수 → 무손실
    assert rmses[1] <= rmses[2] <= rmses[3]      # 절단 늘수록 오차 증가


def test_gradient_flows_to_input():
    fwd = DCT2d(kernel_size=8)
    x = torch.randn(2, 3, 16, 16, requires_grad=True)
    fwd(x).pow(2).sum().backward()
    assert x.grad is not None and x.grad.abs().sum().item() > 0
```

- [ ] **Step 2: 테스트 통과 확인(기존 동작 커버리지)**

Run: `.venv/bin/python -m pytest tests/test_dct.py -q`
Expected: PASS (전체).

- [ ] **Step 3: docstring 추가** — `DCT2d`/`IDCT2d` 클래스 본문 첫 줄

`DCT2d`:

```python
    """Block 2D DCT as a strided convolution.

    Args:
        dct: DCT type passed to scipy (default 2, i.e. DCT-II).
        kernel_size: block side length; input H and W must be divisible by it.
        selections: keep only the first N zigzag (low→high freq) coefficients.
            None keeps all kernel_size**2.
        norm: scipy normalization, one of "ortho" (default, orthonormal /
            energy-preserving), "backward", or "forward".
        device: device for the precomputed kernel buffer.

    Input:  (B, C, H, W) image tensor.
    Output: (B, C * selections, H // kernel_size, W // kernel_size) coefficients,
        per block ordered low→high frequency.
    """
```

`IDCT2d`:

```python
    """Inverse of :class:`DCT2d` via transposed convolution.

    Args mirror :class:`DCT2d`; use the same ``norm``, ``kernel_size`` and
    ``selections`` as the forward transform for exact reconstruction.

    Input:  (B, C * selections, H, W) coefficient tensor.
    Output: (B, C, H * kernel_size, W * kernel_size) reconstruction.
    """
```

- [ ] **Step 4: 메타데이터 갱신**

`dct/__init__.py`: `__version__ = "2.0.0"`.

`pyproject.toml`: `version = "2.0.0"` 로, 그리고 dev 의존성 추가:

```toml
[project.optional-dependencies]
dev = ["pytest"]
```

`README.md`: 마지막 단락 뒤에 한 줄 추가:

```markdown

By default the transform is orthonormal (`norm="ortho"`, energy-preserving); pass `norm="backward"` or `"forward"` for the corresponding SciPy conventions.
```

- [ ] **Step 5: 전체 테스트 + 설치 확인**

Run:
```bash
.venv/bin/python -m pytest tests/ -q
.venv/bin/python -c "import dct; print(dct.__version__)"
```
Expected: 전체 PASS, `2.0.0` 출력.

- [ ] **Step 6: 커밋**

```bash
git add dct/ tests/ pyproject.toml README.md
git commit -m "docs/test: add docstrings, coverage tests, bump to 2.0.0"
```

---

## Self-Review

**Spec 커버리지:** (1) normalization→Task 1, (2) fp16/dtype→Task 3, (3) 구조 정리/죽은 코드/helper/device→Task 2, IDCT 검증→Task 4, (4) 테스트·docstring·메타→Task 1·5. 전 항목 매핑됨.

**Placeholder 스캔:** 모든 코드 스텝에 실제 코드 포함, TBD/TODO 없음. Task 4 Step 1의 잘못된 첫 초안은 바로 아래 "최종 테스트 사용" 블록으로 교체 명시.

**타입 일관성:** `norm` 시그니처·기본값, `_BlockTransform(dct, kernel_size, selections, norm, kernel_fn, transpose, device)` 인자 순서, `_weight(x)`, `_build_block_kernel(matrix_1d, k)`, `selections` 속성이 태스크 전반에서 일치.
